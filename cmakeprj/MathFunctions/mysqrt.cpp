#include <stdio.h>
#include <stdlib.h>
#include <math.h>




int mysqrt (double inputValue, double outputValue)
{
  outputValue = sqrt(inputValue);
  fprintf(stdout,"Now using mysqrt. ¥n");
  return 0;
}
